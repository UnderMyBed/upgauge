-- The Explorer's measure allowlist, as catalog rows. Companion to catalog_dimensions.sql;
-- see that file's header for why this is a file and not a string literal.
SELECT * FROM meta_pivot_measures
